# .env.example
# This file serves as a template for your environment variables.
# Create a copy named '.env' and fill in your desired values.

# --- Model Configuration ---
# Name of the sentence-transformer model to use.
# Options: 'all-MiniLM-L6-v2' (default, English), 'paraphrase-multilingual-mpnet-base-v2' (for multi-language support)
EMBEDDING_MODEL=paraphrase-multilingual-mpnet-base-v2

# Device to run the embedding model on.
# Options: 'cpu' (default), 'cuda' (if GPU is available)
EMBEDDING_DEVICE=cpu
